import { Section, StatContainer, Title } from './GameStats.styled';

export const GameStats = () => {
  return (
    <Section>
      <StatContainer>
        <Title>Game Stats</Title>
      </StatContainer>
      <StatContainer>
        <Title>Game Stats</Title>
      </StatContainer>
    </Section>
  );
};

